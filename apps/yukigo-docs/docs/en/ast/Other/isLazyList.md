# Function: isLazyList()

> **isLazyList**(`prim`): `prim is LazyList`

## Parameters

| Parameter | Type |
| ------ | ------ |
| `prim` | [`PrimitiveValue`](PrimitiveValue.md) |

## Returns

`prim is LazyList`
