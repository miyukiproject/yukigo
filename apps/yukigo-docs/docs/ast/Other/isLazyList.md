# Function: isLazyList()

> **isLazyList**(`prim`): `prim is LazyList`

## Parameters

| Parameter | Type |
| ------ | ------ |
| `prim` | [`PrimitiveValue`](index.PrimitiveValue.md) |

## Returns

`prim is LazyList`
